import "./App.css";
import DynamicForm from "./component/DynamicForm";

// const handleChange = () => {
//   //handle textfield and select tag change logic here based on "NAME"
// };


const App = (props) => {
  return (
    <div className="App">
      <DynamicForm  />
    </div>
  );
};

export default App;
